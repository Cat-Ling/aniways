{
  "$schema": "https://turbo.build/schema.json",
  "globalDotEnv": [".env"],
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "dotEnv": [".env"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dotEnv": [".env"]
    },
    "start": {
      "dotEnv": [".env"],
      "dependsOn": ["^start"]
    },
    "package": {
      "dependsOn": ["lint"],
      "dotEnv": [".env"]
    },
    "deploy:prod": {
      "dependsOn": ["package"],
      "dotEnv": [".env"]
    }
  }
}
